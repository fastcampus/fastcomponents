import React from 'react';
import { css } from '@emotion/react';
import { Player as FCPlayer } from '../../../src';

const Player = () => {
  return (
    <div css={css``}>
      <FCPlayer className="test" />
    </div>
  );
};

export default Player;
